// collection  of same data types
// pre defined data types

var user: string[] = ["jayesh", "tapdiya", "banglore"];
var marks: number[] = [10, 20, 30, 40];

var student: string[] = ["hello", "hiiii"];

student.push("jayesh");

console.log(student);

// we can create the readonly array in which we cannot add the elements;

var cllg: readonly string[] = ["nit", "iit"];

// cllg.push("medicaps")
