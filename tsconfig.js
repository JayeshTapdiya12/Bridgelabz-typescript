// in this file we will see the the tsconfig.json file details and use and how to modify them
// to convert all the ts file into js you only need to code in terminal
// tsc  (if you have the tsconfig.json file in your project)
//  to make sure that all the converted file should be come in a single place we should assign a value to  outdir in the tsconfig
// to save all thhe file there automaticlaly
//  if we make declartion true then we will get all the declartion of the file in a new file with a extension of  .d.ts in the output folder
