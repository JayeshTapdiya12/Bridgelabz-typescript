var user = null;
var login = true;
if (login) {
    user = "hello";
}
console.log(user);
// *******************************************************
var item = undefined;
//  for null == undefiend is true but for the ===  it is false
